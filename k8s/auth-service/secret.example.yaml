# Template — do NOT commit real secrets.
#
# auth-service holds no database, so its only secrets are the signing key it
# shares with the three validating services and the admin API key.
# Run ../secrets-from-tofu.sh to generate secret.yaml (gitignored).
apiVersion: v1
kind: Secret
metadata:
  name: auth-service
  namespace: tech-challenge
type: Opaque
stringData:
  JWT_SECRET: CHANGE_ME
  ADMIN_API_KEY: CHANGE_ME
