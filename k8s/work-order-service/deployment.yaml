apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-order-service
  namespace: tech-challenge
  labels:
    app: work-order-service
    app.kubernetes.io/name: work-order-service
    app.kubernetes.io/part-of: tech-challenge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: work-order-service
  template:
    metadata:
      labels:
        app: work-order-service
        app.kubernetes.io/name: work-order-service
        app.kubernetes.io/part-of: tech-challenge
        tags.datadoghq.com/service: work-order-service
        tags.datadoghq.com/env: production
    spec:
      containers:
        - name: app
          image: ghcr.io/tech-challenge-workshop/work-order-service:main
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: work-order-service
            - secretRef:
                name: work-order-service
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
      restartPolicy: Always
