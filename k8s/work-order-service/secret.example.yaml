# Template — do NOT commit real secrets.
#
# Do not fill this in by hand: run ../secrets-from-tofu.sh, which reads the
# OpenTofu outputs and Secrets Manager and writes secret.yaml (gitignored).
#
# Hosts are the managed AWS endpoints created by terraform/, not in-cluster
# services. DD_API_KEY is deliberately absent: dd-trace ships to the Datadog
# Agent over DD_AGENT_HOST (set in the ConfigMap), and only the Agent holds
# the API key.
apiVersion: v1
kind: Secret
metadata:
  name: work-order-service
  namespace: tech-challenge
type: Opaque
stringData:
  DATABASE_URL: postgresql://workorder:CHANGE_ME@CHANGE_ME.rds.amazonaws.com:5432/workorder?schema=public&sslmode=require
  RABBITMQ_URL: amqps://tech_challenge:CHANGE_ME@CHANGE_ME.mq.us-east-1.amazonaws.com:5671
  JWT_SECRET: CHANGE_ME
