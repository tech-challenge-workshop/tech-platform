# Template — do NOT commit real secrets.
#
# Do not fill this in by hand: run ../secrets-from-tofu.sh, which reads the
# OpenTofu outputs and Secrets Manager and writes secret.yaml (gitignored).
#
# MERCADO_PAGO_ACCESS_TOKEN is the one value the script cannot derive. Leave it
# empty to use the sandbox adapter, which auto-approves payments.
apiVersion: v1
kind: Secret
metadata:
  name: billing-service
  namespace: tech-challenge
type: Opaque
stringData:
  DATABASE_URL: postgresql://billing:CHANGE_ME@CHANGE_ME.rds.amazonaws.com:5432/billing?schema=public&sslmode=require
  RABBITMQ_URL: amqps://tech_challenge:CHANGE_ME@CHANGE_ME.mq.us-east-1.amazonaws.com:5671
  JWT_SECRET: CHANGE_ME
  MERCADO_PAGO_ACCESS_TOKEN: ""
