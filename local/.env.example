# Datadog Agent — leave DD_API_KEY blank to run the agent in "offline" mode
# (traces/metrics are dropped, but no crashes). Set it to your key to actually
# send data to Datadog.
DD_API_KEY=
DD_SITE=datadoghq.com
DD_ENV=development
